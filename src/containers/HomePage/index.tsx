import React, { FC, ReactElement } from "react";
import "./index.css";
import Playground from "../../components/Playground";

const HomePage: FC = (): ReactElement => <Playground />;

export default HomePage;
